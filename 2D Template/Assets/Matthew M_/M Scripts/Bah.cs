using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bah : MonoBehaviour
{
    public int buh;
    public bool beh;
    public float bih;

    // Update is called once per frame
    void Start()
    {
        if (buh == bih && beh == false)
        {
            Console.WriteLine("Don't worry. Funky Kong lives. He is in a secret place, and may not have much longer if they get him. Save him, please. You're our only hope.");
        }
    }
}
